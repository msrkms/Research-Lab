@model IEnumerable<Research_Lab.Models.LabCostRate>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAuthorizedamin.cshtml";
}


<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.costperminitue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResearchLab.LabName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.costperminitue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResearchLab.LabName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
